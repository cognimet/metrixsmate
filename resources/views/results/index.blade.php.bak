@extends('layouts.app')

@section('title', 'Assessment Results - Psychometric Portal')

@section('header')
    <div class="flex items-center justify-between">
        <div>
            <h1 class="text-3xl font-bold text-gray-900">Your Assessment Results</h1>
            <p class="mt-2 text-gray-600">Comprehensive insights into your personality, interests, and cognitive abilities</p>
        </div>
        <div class="flex space-x-3">
            <a href="{{ route('results.download', ['quiz' => 1]) }}" class="bg-brandSky hover:bg-blue-600 text-white px-4 py-2 rounded-lg font-medium transition-colors">
                Download Report
            </a>
            <button class="border border-gray-300 hover:border-gray-400 text-gray-700 px-4 py-2 rounded-lg font-medium transition-colors">
                Share Results
            </button>
        </div>
    </div>
@endsection

@section('content')
<!-- Results Overview Cards -->
<div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-8">
    @php
        $overallPersonality = $oceanResults['domains']->avg('percentage');
        $personalityLevel = $overallPersonality >= 85 ? 'exceptional' : ($overallPersonality >= 70 ? 'high' : ($overallPersonality >= 40 ? 'average' : ($overallPersonality >= 20 ? 'below-average' : 'low')));
        $personalityColors = [
            'exceptional' => 'text-green-600',
            'high' => 'text-blue-600', 
            'average' => 'text-amber-600',
            'below-average' => 'text-orange-600',
            'low' => 'text-red-600'
        ];
        
        $overallCognitive = $cognitiveResults['average_score'];
        $cognitiveLevel = $overallCognitive >= 85 ? 'exceptional' : ($overallCognitive >= 70 ? 'high' : ($overallCognitive >= 40 ? 'average' : ($overallCognitive >= 20 ? 'below-average' : 'low')));
    @endphp

    <div class="bg-white rounded-xl shadow-sm border border-gray-100 p-6">
        <div class="flex items-center">
            <div class="bg-blue-100 rounded-lg p-3">
                <svg class="w-6 h-6 text-brandBlue" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"></path>
                </svg>
            </div>
            <div class="ml-4">
                <h3 class="text-lg font-semibold text-gray-900">Personality Profile</h3>
                <p class="text-sm text-gray-600">OCEAN Assessment Complete</p>
            </div>
        </div>
        <div class="mt-4">
            <div class="text-2xl font-bold {{ $personalityColors[$personalityLevel] }}">{{ round($overallPersonality, 0) }}%</div>
            <div class="text-sm text-gray-500">Average across all domains</div>
        </div>
    </div>

    <div class="bg-white rounded-xl shadow-sm border border-gray-100 p-6">
        <div class="flex items-center">
            <div class="bg-green-100 rounded-lg p-3">
                <svg class="w-6 h-6 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 13.255A23.931 23.931 0 0112 15c-3.183 0-6.22-.62-9-1.745M16 6V4a2 2 0 00-2-2h-4a2 2 0 00-2 2v2m4 6h.01M5 20h14a2 2 0 002-2V8a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"></path>
                </svg>
            </div>
            <div class="ml-4">
                <h3 class="text-lg font-semibold text-gray-900">Career Interests</h3>
                <p class="text-sm text-gray-600">RIASEC Assessment Complete</p>
            </div>
        </div>
        <div class="mt-4">
            <div class="text-2xl font-bold text-green-600">{{ $riasecResults['holland_code'] }}</div>
            <div class="text-sm text-gray-500">Top career profile code</div>
        </div>
    </div>

    <div class="bg-white rounded-xl shadow-sm border border-gray-100 p-6">
        <div class="flex items-center">
            <div class="bg-purple-100 rounded-lg p-3">
                <svg class="w-6 h-6 text-purple-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9.663 17h4.673M12 3v1m6.364 1.636l-.707.707M21 12h-1M4 12H3m3.343-5.657l-.707-.707m2.828 9.9a5 5 0 117.072 0l-.548.547A3.374 3.374 0 0014 18.469V19a2 2 0 11-4 0v-.531c0-.895-.356-1.754-.988-2.386l-.548-.547z"></path>
                </svg>
            </div>
            <div class="ml-4">
                <h3 class="text-lg font-semibold text-gray-900">Cognitive Ability</h3>
                <p class="text-sm text-gray-600">Cognitive Assessment Complete</p>
            </div>
        </div>
        <div class="mt-4">
            <div class="text-2xl font-bold {{ $personalityColors[$cognitiveLevel] }}">{{ round($overallCognitive, 0) }}%</div>
            <div class="text-sm text-gray-500">Average cognitive performance</div>
        </div>
    </div>
</div>

<!-- OCEAN Personality Assessment -->
<div class="bg-white rounded-xl shadow-sm border border-gray-100 mb-8">
    <div class="border-b border-gray-200 p-6">
        <h2 class="text-2xl font-bold text-gray-900 flex items-center">
            <span class="bg-blue-100 rounded-lg p-2 mr-3">
                <svg class="w-6 h-6 text-brandBlue" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"></path>
                </svg>
            </span>
            Personality Profile (OCEAN)
        </h2>
        <p class="text-gray-600 mt-2">Your Big Five personality traits and their implications for work and life</p>
    </div>

    <!-- Personality Domains -->
    <div class="p-6">
        <h3 class="text-lg font-semibold text-gray-900 mb-4">Core Personality Domains</h3>
        <div class="space-y-6">
            @foreach($oceanResults['domains'] as $domain)
            <div class="border {{ $domain->colors['border'] }} rounded-lg p-4 {{ $domain->colors['bg'] }} hover:shadow-md transition-shadow">
                <div class="flex items-center justify-between mb-3">
                    <h4 class="text-lg font-medium text-gray-900">{{ $domain->name }}</h4>
                    <div class="flex items-center space-x-2">
                        <span class="text-sm font-medium {{ $domain->colors['text'] }}">{{ round($domain->percentage, 0) }}%</span>
                        <span class="px-2 py-1 {{ $domain->colors['badge'] }} text-xs font-medium rounded-full">{{ $domain->performance_text }}</span>
                    </div>
                </div>
                <div class="w-full bg-gray-200 rounded-full h-2 mb-3">
                    <div class="{{ $domain->colors['progress'] }} h-2 rounded-full transition-all duration-500" style="width: {{ $domain->percentage }}%"></div>
                </div>
                <p class="text-gray-600 text-sm mb-2">{{ $domain->description }}</p>
                <div class="{{ $domain->colors['bg'] }} border-l-4 {{ $domain->colors['border'] }} p-3 rounded">
                    <p class="text-sm {{ $domain->colors['text'] }} font-medium">Actionable Insight:</p>
                    <p class="text-sm text-gray-700 mt-1">{{ $domain->actionable_insights }}</p>
                </div>
            </div>
            @endforeach
        </div>
    </div>

    <!-- Predictive Insights -->
    <div class="border-t border-gray-200 p-6">
        <h3 class="text-lg font-semibold text-gray-900 mb-4">Predictive Insights</h3>
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-4">
            @if(isset($oceanResults['predictive_insights']['growth_potential']))
            @php $insight = $oceanResults['predictive_insights']['growth_potential']; @endphp
            <div class="bg-gradient-to-br {{ $insight->colors['bg'] }} to-{{ str_replace('bg-', '', $insight->colors['bg']) }} rounded-lg p-4 border {{ $insight->colors['border'] }}">
                <div class="text-2xl font-bold {{ $insight->colors['text'] }}">{{ round($insight->percentage, 0) }}%</div>
                <div class="text-sm font-medium {{ $insight->colors['text'] }}">Growth Potential</div>
                <div class="text-xs {{ $insight->colors['text'] }} mt-1">{{ $insight->performance_text }} capacity</div>
            </div>
            @endif
            
            @if(isset($oceanResults['predictive_insights']['organizational_fit_forecast']))
            @php $insight = $oceanResults['predictive_insights']['organizational_fit_forecast']; @endphp
            <div class="bg-gradient-to-br {{ $insight->colors['bg'] }} to-{{ str_replace('bg-', '', $insight->colors['bg']) }} rounded-lg p-4 border {{ $insight->colors['border'] }}">
                <div class="text-2xl font-bold {{ $insight->colors['text'] }}">{{ round($insight->percentage, 0) }}%</div>
                <div class="text-sm font-medium {{ $insight->colors['text'] }}">Organizational Fit</div>
                <div class="text-xs {{ $insight->colors['text'] }} mt-1">{{ $insight->performance_text }} alignment</div>
            </div>
            @endif
            
            @if(isset($oceanResults['predictive_insights']['leadership_potential']))
            @php $insight = $oceanResults['predictive_insights']['leadership_potential']; @endphp
            <div class="bg-gradient-to-br {{ $insight->colors['bg'] }} to-{{ str_replace('bg-', '', $insight->colors['bg']) }} rounded-lg p-4 border {{ $insight->colors['border'] }}">
                <div class="text-2xl font-bold {{ $insight->colors['text'] }}">{{ round($insight->percentage, 0) }}%</div>
                <div class="text-sm font-medium {{ $insight->colors['text'] }}">Leadership Potential</div>
                <div class="text-xs {{ $insight->colors['text'] }} mt-1">{{ $insight->performance_text }} capabilities</div>
            </div>
            @endif
            
            @if(isset($oceanResults['predictive_insights']['innovation_index']))
            @php $insight = $oceanResults['predictive_insights']['innovation_index']; @endphp
            <div class="bg-gradient-to-br {{ $insight->colors['bg'] }} to-{{ str_replace('bg-', '', $insight->colors['bg']) }} rounded-lg p-4 border {{ $insight->colors['border'] }}">
                <div class="text-2xl font-bold {{ $insight->colors['text'] }}">{{ round($insight->percentage, 0) }}%</div>
                <div class="text-sm font-medium {{ $insight->colors['text'] }}">Innovation Index</div>
                <div class="text-xs {{ $insight->colors['text'] }} mt-1">{{ $insight->performance_text }} creativity</div>
            </div>
            @endif
        </div>
    </div>
</div>

<!-- RIASEC Career Interests -->
<div class="bg-white rounded-xl shadow-sm border border-gray-100 mb-8">
    <div class="border-b border-gray-200 p-6">
        <h2 class="text-2xl font-bold text-gray-900 flex items-center">
            <span class="bg-green-100 rounded-lg p-2 mr-3">
                <svg class="w-6 h-6 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 13.255A23.931 23.931 0 0112 15c-3.183 0-6.22-.62-9-1.745M16 6V4a2 2 0 00-2-2h-4a2 2 0 00-2 2v2m4 6h.01M5 20h14a2 2 0 002-2V8a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"></path>
                </svg>
            </span>
            Career Interest Profile (RIASEC)
        </h2>
        <p class="text-gray-600 mt-2">Discover careers that align with your interests and work preferences</p>
    </div>

    <div class="p-6">
        <!-- Career Interest Domains -->
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4 mb-6">
            @php
            $riasecColors = [
                'Realistic' => 'bg-red-100 text-red-800',
                'Investigative' => 'bg-blue-100 text-blue-800',
                'Artistic' => 'bg-purple-100 text-purple-800',
                'Social' => 'bg-green-100 text-green-800',
                'Enterprising' => 'bg-yellow-100 text-yellow-800',
                'Conventional' => 'bg-gray-100 text-gray-800'
            ];
            @endphp

            @foreach($riasecResults['domains'] as $domain)
            <div class="border {{ $domain->colors['border'] }} rounded-lg p-4 text-center {{ $domain->colors['bg'] }} hover:shadow-md transition-shadow">
                <div class="w-12 h-12 {{ $riasecColors[$domain->name] ?? 'bg-gray-100 text-gray-800' }} rounded-full flex items-center justify-center mx-auto mb-3">
                    <span class="text-lg font-bold">{{ strtoupper(substr($domain->name, 0, 1)) }}</span>
                </div>
                <h4 class="font-medium text-gray-900 mb-1">{{ $domain->name }}</h4>
                <div class="text-2xl font-bold {{ $domain->colors['text'] }} mb-1">{{ round($domain->percentage, 0) }}%</div>
                <div class="w-full bg-gray-200 rounded-full h-1.5 mb-2">
                    <div class="{{ $domain->colors['progress'] }} h-1.5 rounded-full transition-all duration-500" style="width: {{ $domain->percentage }}%"></div>
                </div>
                <span class="px-2 py-1 {{ $domain->colors['badge'] }} text-xs font-medium rounded-full">{{ $domain->performance_text }}</span>
            </div>
            @endforeach
        </div>

        <!-- Career Recommendations -->
        <div class="bg-green-50 border border-green-200 rounded-lg p-6">
            <h3 class="text-lg font-semibold text-green-900 mb-3">Recommended Career Paths</h3>
            <div class="text-sm text-green-800 mb-2">Based on your <strong>{{ $riasecResults['holland_code'] }}</strong> profile:</div>
            @if($riasecResults['career_analysis'])
            <div class="flex flex-wrap gap-2 mb-4">
                @foreach(explode(';', $riasecResults['career_analysis']->level_description) as $career)
                <span class="bg-white border border-green-300 text-green-800 px-3 py-1 rounded-full text-sm font-medium">{{ trim($career) }}</span>
                @endforeach
            </div>
            @endif
            
            <h4 class="font-medium text-green-900 mb-2">Ideal Work Environments:</h4>
            @if($riasecResults['work_environment'])
            <ul class="text-sm text-green-700 space-y-1">
                @foreach(explode(';', $riasecResults['work_environment']->level_description) as $environment)
                <li>• {{ trim($environment) }}</li>
                @endforeach
            </ul>
            @endif
        </div>
    </div>
</div>

<!-- Cognitive Assessment -->
<div class="bg-white rounded-xl shadow-sm border border-gray-100 mb-8">
    <div class="border-b border-gray-200 p-6">
        <h2 class="text-2xl font-bold text-gray-900 flex items-center">
            <span class="bg-purple-100 rounded-lg p-2 mr-3">
                <svg class="w-6 h-6 text-purple-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9.663 17h4.673M12 3v1m6.364 1.636l-.707.707M21 12h-1M4 12H3m3.343-5.657l-.707-.707m2.828 9.9a5 5 0 117.072 0l-.548.547A3.374 3.374 0 0014 18.469V19a2 2 0 11-4 0v-.531c0-.895-.356-1.754-.988-2.386l-.548-.547z"></path>
                </svg>
            </span>
            Cognitive Abilities
        </h2>
        <p class="text-gray-600 mt-2">Your cognitive strengths across different reasoning domains</p>
    </div>

    <div class="p-6">
        <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
            @foreach($cognitiveResults['domains'] as $cognitive)
            <div class="border {{ $cognitive->colors['border'] }} rounded-lg p-4 {{ $cognitive->colors['bg'] }}">
                <div class="flex items-center justify-between mb-3">
                    <h4 class="text-lg font-medium text-gray-900">{{ $cognitive->name }}</h4>
                    <span class="px-3 py-1 {{ $cognitive->colors['badge'] }} text-sm font-medium rounded-full">{{ $cognitive->performance_text }}</span>
                </div>
                <div class="flex items-center space-x-4">
                    <div class="flex-1">
                        <div class="w-full bg-gray-200 rounded-full h-3">
                            <div class="{{ $cognitive->colors['progress'] }} h-3 rounded-full transition-all duration-500" style="width: {{ $cognitive->percentage }}%"></div>
                        </div>
                    </div>
                    <div class="text-2xl font-bold {{ $cognitive->colors['text'] }}">{{ round($cognitive->percentage, 0) }}%</div>
                </div>
                <p class="text-sm text-gray-600 mt-2">You demonstrate {{ strtolower($cognitive->performance_text) }} capabilities in {{ $cognitive->name }} tasks</p>
            </div>
            @endforeach
        </div>

        <div class="mt-6 bg-purple-50 border border-purple-200 rounded-lg p-6">
            <h3 class="text-lg font-semibold text-purple-900 mb-3">Cognitive Strengths Summary</h3>
            <p class="text-purple-800 mb-3">Your cognitive profile shows {{ $cognitiveResults['domains']->where('percentage', '>=', 85)->count() > 0 ? 'exceptional performance' : ($cognitiveResults['domains']->where('percentage', '>=', 70)->count() > 0 ? 'strong performance' : 'good performance') }} across cognitive domains.</p>
            <div class="text-sm text-purple-700">
                <strong>Key Strengths:</strong> {{ $cognitiveResults['domains']->sortByDesc('percentage')->take(2)->pluck('name')->implode(', ') }}
            </div>
        </div>
    </div>
</div>

<!-- Action Plan -->
<div class="bg-gradient-to-br from-brandBlue to-blue-700 rounded-xl text-white p-8">
    <h2 class="text-2xl font-bold mb-4">Your Personalized Development Plan</h2>
    <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
        <div class="bg-white/10 backdrop-blur rounded-lg p-6">
            <h3 class="text-lg font-semibold mb-3">Immediate Focus</h3>
            <ul class="space-y-2 text-sm">
                <li>• Develop systematic work approaches</li>
                <li>• Balance social and focused work time</li>
                <li>• Practice stress management techniques</li>
            </ul>
        </div>
        <div class="bg-white/10 backdrop-blur rounded-lg p-6">
            <h3 class="text-lg font-semibold mb-3">Growth Areas</h3>
            <ul class="space-y-2 text-sm">
                <li>• Leadership development programs</li>
                <li>• Creative problem-solving workshops</li>
                <li>• Technical skill enhancement</li>
            </ul>
        </div>
        <div class="bg-white/10 backdrop-blur rounded-lg p-6">
            <h3 class="text-lg font-semibold mb-3">Career Opportunities</h3>
            <ul class="space-y-2 text-sm">
                @if($riasecResults['career_analysis'])
                    @foreach(array_slice(explode(';', $riasecResults['career_analysis']->level_description), 0, 3) as $career)
                        <li>• {{ trim($career) }}</li>
                    @endforeach
                @else
                    <li>• Software engineering roles</li>
                    <li>• Research and analysis positions</li>
                    <li>• Product development teams</li>
                @endif
            </ul>
        </div>
    </div>
</div>

<script>
// Add smooth animations for progress bars
document.addEventListener('DOMContentLoaded', function() {
    const progressBars = document.querySelectorAll('[style*="width:"]');
    progressBars.forEach(bar => {
        const width = bar.style.width;
        bar.style.width = '0%';
        setTimeout(() => {
            bar.style.width = width;
        }, 100);
    });
});
</script>
@endsection