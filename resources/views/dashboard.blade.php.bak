@extends('layouts.app')

@section('title', 'Dashboard - MindMetrics')

@section('content')
<div class="space-y-8">
    <!-- Welcome Section -->
    <div class="bg-gradient-to-r from-indigo-600 to-purple-600 rounded-2xl shadow-xl p-8 text-white">
        <div class="flex items-center justify-between">
            <div>
                <h1 class="text-3xl md:text-4xl font-extrabold mb-3">
                    Welcome back, {{ auth()->user()->name }}! 👋
                </h1>
                <p class="text-indigo-100 text-lg">
                    Ready to continue your journey of self-discovery? Complete your assessments below.
                </p>
            </div>
            <div class="hidden md:block">
                <svg class="w-24 h-24 text-white/20" fill="currentColor" viewBox="0 0 20 20">
                    <path d="M9 4.804A7.968 7.968 0 005.5 4c-1.255 0-2.443.29-3.5.804v10A7.969 7.969 0 015.5 14c1.669 0 3.218.51 4.5 1.385A7.962 7.962 0 0114.5 14c1.255 0 2.443.29 3.5.804v-10A7.968 7.968 0 0014.5 4c-1.255 0-2.443.29-3.5.804V12a1 1 0 11-2 0V4.804z"/>
                </svg>
            </div>
        </div>
    </div>

    <!-- Progress Overview -->
    <div class="bg-white rounded-2xl shadow-lg p-6">
        <div class="flex items-center justify-between mb-6">
            <h2 class="text-2xl font-bold text-gray-900">Your Progress</h2>
            @php
                $totalAssessments = 3;
                $completedCount = 0;
                if (auth()->user()->is_ocean_assessment_completed) $completedCount++;
                if (auth()->user()->is_riasec_assessment_completed) $completedCount++;
                if (auth()->user()->is_cognitive_assessment_completed) $completedCount++;
                $progressPercentage = ($completedCount / $totalAssessments) * 100;
            @endphp
            <span class="text-sm font-semibold text-indigo-600">{{ $completedCount }}/{{ $totalAssessments }} Complete</span>
        </div>
        <div class="w-full bg-gray-200 rounded-full h-3 mb-2">
            <div class="bg-gradient-to-r from-indigo-600 to-purple-600 h-3 rounded-full transition-all duration-500" style="width: {{ $progressPercentage }}%"></div>
        </div>
        <p class="text-sm text-gray-600">
            @if($completedCount === 0)
                Let's get started! Begin your first assessment below.
            @elseif($completedCount < $totalAssessments)
                Great progress! {{ $totalAssessments - $completedCount }} assessment{{ $totalAssessments - $completedCount > 1 ? 's' : '' }} remaining.
            @else
                🎉 Congratulations! You've completed all assessments!
            @endif
        </p>
    </div>

    <!-- Assessments Grid -->
    @if(isset($quizzes) && $quizzes->count())
        <div>
            <h2 class="text-2xl font-bold text-gray-900 mb-6">Available Assessments</h2>
            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
                @foreach($quizzes as $quiz)
                    @php
                        $user = auth()->user();
                        $completed = false;
                        $icon = '';
                        $gradient = '';
                        
                        if (strtolower($quiz->title) === 'ocean') {
                            $completed = $user->is_ocean_assessment_completed;
                            $icon = 'M14.828 14.828a4 4 0 01-5.656 0M9 10h.01M15 10h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z';
                            $gradient = 'from-green-400 to-emerald-600';
                        } elseif (strtolower($quiz->title) === 'riasec') {
                            $completed = $user->is_riasec_assessment_completed;
                            $icon = 'M21 13.255A23.931 23.931 0 0112 15c-3.183 0-6.22-.62-9-1.745M16 6V4a2 2 0 00-2-2h-4a2 2 0 00-2 2v2m4 6h.01M5 20h14a2 2 0 002-2V8a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z';
                            $gradient = 'from-blue-400 to-indigo-600';
                        } elseif (strtolower($quiz->title) === 'cognitive') {
                            $completed = $user->is_cognitive_assessment_completed;
                            $icon = 'M9.663 17h4.673M12 3v1m6.364 1.636l-.707.707M21 12h-1M4 12H3m3.343-5.657l-.707-.707m2.828 9.9a5 5 0 117.072 0l-.548.547A3.374 3.374 0 0014 18.469V19a2 2 0 11-4 0v-.531c0-.895-.356-1.754-.988-2.386l-.548-.547z';
                            $gradient = 'from-purple-400 to-pink-600';
                        }
                    @endphp
                    
                    <div class="bg-white rounded-2xl shadow-lg hover:shadow-xl transition-all transform hover:-translate-y-1 overflow-hidden">
                        <!-- Card Header with Icon -->
                        <div class="bg-gradient-to-br {{ $gradient }} p-6">
                            <div class="flex items-center justify-between mb-4">
                                <div class="w-14 h-14 bg-white/20 backdrop-blur-sm rounded-xl flex items-center justify-center">
                                    <svg class="w-8 h-8 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="{{ $icon }}"/>
                                    </svg>
                                </div>
                                @if($completed)
                                    <div class="bg-white/20 backdrop-blur-sm px-3 py-1 rounded-full">
                                        <span class="text-white text-sm font-semibold flex items-center">
                                            <svg class="w-4 h-4 mr-1" fill="currentColor" viewBox="0 0 20 20">
                                                <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"/>
                                            </svg>
                                            Completed
                                        </span>
                                    </div>
                                @endif
                            </div>
                            <h3 class="text-2xl font-bold text-white">{{ $quiz->title }}</h3>
                        </div>

                        <!-- Card Body -->
                        <div class="p-6">
                            <p class="text-gray-600 mb-6 leading-relaxed">{{ $quiz->description }}</p>
                            
                            @if($completed)
                                <div class="flex gap-3">
                                    <a href="{{ route('results.show', $quiz->id) }}" 
                                       class="flex-1 text-center px-4 py-3 bg-gradient-to-r from-indigo-600 to-purple-600 text-white rounded-lg font-semibold shadow-md hover:shadow-lg transform hover:-translate-y-0.5 transition-all">
                                        View Results
                                    </a>
                                    <a href="{{ route('assessments.showAll', $quiz->id) }}" 
                                       class="px-4 py-3 border-2 border-gray-300 text-gray-700 rounded-lg font-semibold hover:border-indigo-600 hover:text-indigo-600 transition">
                                        Retake
                                    </a>
                                </div>
                            @else
                                <a href="{{ route('assessments.showAll', $quiz->id) }}"
                                   class="block text-center px-6 py-3 bg-gradient-to-r from-indigo-600 to-purple-600 text-white rounded-lg font-semibold shadow-md hover:shadow-lg transform hover:-translate-y-0.5 transition-all">
                                    <span class="flex items-center justify-center">
                                        <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M14.752 11.168l-3.197-2.132A1 1 0 0010 9.87v4.263a1 1 0 001.555.832l3.197-2.132a1 1 0 000-1.664z"/>
                                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/>
                                        </svg>
                                        Start Assessment
                                    </span>
                                </a>
                            @endif
                        </div>
                    </div>
                @endforeach
            </div>
        </div>

        <!-- Results Button -->
        @php
            $allCompleted = auth()->user()->is_ocean_assessment_completed &&
                            auth()->user()->is_riasec_assessment_completed &&
                            auth()->user()->is_cognitive_assessment_completed;
        @endphp

        @if($allCompleted)
            <div class="bg-gradient-to-r from-green-50 to-emerald-50 border-2 border-green-200 rounded-2xl p-8 text-center">
                <div class="inline-flex items-center justify-center w-16 h-16 bg-green-500 rounded-full mb-4">
                    <svg class="w-8 h-8 text-white" fill="currentColor" viewBox="0 0 20 20">
                        <path fill-rule="evenodd" d="M6.267 3.455a3.066 3.066 0 001.745-.723 3.066 3.066 0 013.976 0 3.066 3.066 0 001.745.723 3.066 3.066 0 012.812 2.812c.051.643.304 1.254.723 1.745a3.066 3.066 0 010 3.976 3.066 3.066 0 00-.723 1.745 3.066 3.066 0 01-2.812 2.812 3.066 3.066 0 00-1.745.723 3.066 3.066 0 01-3.976 0 3.066 3.066 0 00-1.745-.723 3.066 3.066 0 01-2.812-2.812 3.066 3.066 0 00-.723-1.745 3.066 3.066 0 010-3.976 3.066 3.066 0 00.723-1.745 3.066 3.066 0 012.812-2.812zm7.44 5.252a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"/>
                    </svg>
                </div>
                <h3 class="text-2xl font-bold text-gray-900 mb-2">All Assessments Complete! 🎉</h3>
                <p class="text-gray-600 mb-6">You've completed all three assessments. View your comprehensive results now.</p>
                <a href="{{ route('results.index') }}"
                   class="inline-flex items-center px-8 py-4 bg-gradient-to-r from-indigo-600 to-purple-600 text-white rounded-lg font-bold shadow-lg hover:shadow-xl transform hover:-translate-y-1 transition-all">
                    <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z"/>
                    </svg>
                    View Complete Results
                </a>
            </div>
        @else
            <div class="bg-blue-50 border-2 border-blue-200 rounded-2xl p-6">
                <div class="flex items-start">
                    <div class="flex-shrink-0">
                        <svg class="w-6 h-6 text-blue-600" fill="currentColor" viewBox="0 0 20 20">
                            <path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7-4a1 1 0 11-2 0 1 1 0 012 0zM9 9a1 1 0 000 2v3a1 1 0 001 1h1a1 1 0 100-2v-3a1 1 0 00-1-1H9z" clip-rule="evenodd"/>
                        </svg>
                    </div>
                    <div class="ml-3">
                        <h3 class="text-sm font-semibold text-blue-900">Complete all assessments to unlock your results</h3>
                        <p class="mt-1 text-sm text-blue-700">You need to complete all three assessments before you can view your comprehensive results and insights.</p>
                    </div>
                </div>
            </div>
        @endif
    @else
        <div class="bg-white rounded-2xl shadow-lg p-12 text-center">
            <div class="inline-flex items-center justify-center w-16 h-16 bg-gray-100 rounded-full mb-4">
                <svg class="w-8 h-8 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M20 13V6a2 2 0 00-2-2H6a2 2 0 00-2 2v7m16 0v5a2 2 0 01-2 2H6a2 2 0 01-2-2v-5m16 0h-2.586a1 1 0 00-.707.293l-2.414 2.414a1 1 0 01-.707.293h-3.172a1 1 0 01-.707-.293l-2.414-2.414A1 1 0 006.586 13H4"/>
                </svg>
            </div>
            <h3 class="text-xl font-semibold text-gray-900 mb-2">No Assessments Available</h3>
            <p class="text-gray-500">Please contact support if you believe this is an error.</p>
        </div>
    @endif
</div>
@endsection
